
import { pit } from '../../../util/spec-extensions';

describe('Substitutor', () => {

  describe('substitute()', () => {

    describe('domain', () => {
      beforeEach(() => {
      });

      let sample1 = () => [
      ];

      pit('should', sample1, (data) => {
      });

    });

  });

});
