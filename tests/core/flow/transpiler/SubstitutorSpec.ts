
import { pit } from '../../../util/extensions';

describe('Substitutor', () => {

  describe('substitute()', () => {

    describe('domain', () => {
      beforeEach(() => {
      });

      let sample1 = () => [
      ];

      pit('should', sample1, (data) => {
      });

    });

  });

});
