native domain Any {
    trait Is is (other: Any);
    trait Not is (other: Any);
    trait Void is Equal();

    native domain Type<T> is {{
            type: T,
            cast(value: Any)?: T,
        }

        to String;
    }
}
