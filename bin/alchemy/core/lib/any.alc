native domain Any {
    trait Is is (other: Any) => this == other;
    trait Not is (other: Any) => !other;
    trait Void is Equal();

    native domain Type<T> is {{
            type: T;
            native cast(value: Any)?: T;
        }

        to String;
    }
}
